const config = {
  provider: "alechp",
  repospacePath: __dirname,
  repositories: [
    { servexyz: "node-starter" },
    { servexyz: "npm-starter-sample-module" }
  ]
}

module.exports = { config };

